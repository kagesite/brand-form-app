import React from 'react'

function Step5({ func }) {
    return (
        <div>
            <h1>Finished</h1>
            <button onClick={func}>Go back home</button>
        </div>
    )
}

export default Step5
